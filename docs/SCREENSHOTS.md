# Screenshots

## Browser UI

![Browser UI overview](assets/control-ui.png)

The screenshot was generated from the local FastAPI server running the static web UI in a disconnected state. This is intentional: it documents the default public-facing interface without requiring BLE hardware in CI or documentation builds.
