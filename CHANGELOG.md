# Changelog

## Unreleased

- switched the coyopy dependency to a GitHub VCS requirement instead of a sibling checkout assumption
- updated CI to install dependencies directly and type-check only the published package
- expanded README and docs with installation, API, development and screenshot coverage
- captured a browser UI screenshot for public-facing documentation
- tightened ignore rules for caches, coverage output and build artifacts

## 0.1.0

- extracted standalone API server project
- removed bundled games from server package and browser UI
- added independent packaging, requirements, CI and API documentation scaffold
